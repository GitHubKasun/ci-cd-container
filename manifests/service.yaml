apiVersion: v1
kind: Service
metadata:
  name: aks-auto
  namespace: aks-python-app
  labels:
    app.kubernetes.io/name: aks-auto
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: aks-auto
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80